---
pair_id: WO-QPC-010
lane: CURSOR
scheduled_utc: 2026-05-26T18:30:00Z
execution_authority: cursor_agent
expected_mode: agent
---

# WO-QPC-010 — Migration & deploy (Cursor)

## Task

1. Configure staging host `qpc.shaneturon.ca` on Cloudflare Pages/Workers (wrangler env `staging`).  
2. Document future domain cutover checklist (`qpcclinic.ca` / `quebecpsychologyclinic.ca`).  
3. Queue first integrated build for NotebookLM pipeline:

```bash
# After operator approval:
~/ShipYard/scripts/process-notebooklm-work-order.sh --dry-run
# Move WO-QPC-010 summary to build-local-server when ready
```

## Deploy gate (mandatory)

- Run `npm run demo:local` on execution repo  
- Operator reviews tunnel via `NOTEBOOKLM_CONTEXT/00_LIVE_ENTRY.md`  
- Copy approved order file to `Deployed/` before `npm run deploy`  

## Acceptance

- [ ] Staging URL serves QPC shell  
- [ ] Execution log row: `WO-QPC-010 | SUCCESS`  
- [ ] No deploy to production domain without `Deployed/` file  

**Paired:** `WO-QPC-010_20260526T183000Z_HUMAN.md`
