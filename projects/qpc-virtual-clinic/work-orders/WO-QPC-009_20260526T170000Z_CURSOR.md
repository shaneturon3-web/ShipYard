---
pair_id: WO-QPC-009
lane: CURSOR
scheduled_utc: 2026-05-26T17:00:00Z
execution_authority: cursor_agent
expected_mode: agent
---

# WO-QPC-009 — Protected operations routes (Cursor)

## Task

1. Implement routes under auth perimeter (reuse ShipYard `perimeter` patterns where applicable).  
2. Add `X-Robots-Tag: noindex` + robots.txt disallow for `/admin`, `/ops`, `/workflows`, `/analytics`, `/triage`.  
3. `/triage` — staff view of triage queue (read-only to PsyNova outputs).  
4. `/analytics` — behind `QPC_AI_ASSIST_ENABLED` + PILOT flag.

## Acceptance

- [ ] Unauthenticated request → 401/redirect login  
- [ ] Public sitemap excludes protected paths  

**Paired:** `WO-QPC-009_20260526T170000Z_HUMAN.md`
