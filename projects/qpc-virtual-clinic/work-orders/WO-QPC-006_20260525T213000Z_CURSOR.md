---
pair_id: WO-QPC-006
lane: CURSOR
scheduled_utc: 2026-05-25T21:30:00Z
execution_authority: cursor_agent
expected_mode: agent
---

# WO-QPC-006 — AI policy enforcement (Cursor)

## Task

1. Add `POLICY_AI_ASSISTANCE.md` in QPC project root (mirror to operator pack on request).  
2. Gate PILOT features behind env flag `QPC_AI_ASSIST_ENABLED=false` default.  
3. Strip any AI marketing strings from templates via lint script or CI check.  
4. Admin-only routes for summarization hooks — require auth perimeter (WO-QPC-009).

## Code rules

- No patient-facing chatbot component in QPC public bundle  
- LLM calls only from `/ops` or `/workflows` namespaces  

## Acceptance

- [ ] `grep -r` public pages: no “AI therapist”, “chatbot therapy”, “powered by AI”  
- [ ] Feature flag documented in README  

**Paired:** `WO-QPC-006_20260525T213000Z_HUMAN.md`
